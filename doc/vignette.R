## ---- include = FALSE---------------------------------------------------------
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)

## ----setup--------------------------------------------------------------------
library(DATED)

## -----------------------------------------------------------------------------
str(snp_dat)

## -----------------------------------------------------------------------------
M <- 5 # leaf size
L <- 3 # layer number
alpha <- 0.05 # desired FDR

## -----------------------------------------------------------------------------
# Model consider covariates effect:
p.leaf <- Test_Leaf(snp_dat=snp_dat,thresh_val=M,covars=c("X1","X2"),teststat="score")

## -----------------------------------------------------------------------------
str(p.leaf)

## -----------------------------------------------------------------------------
out <- DATED(struct_map=p.leaf,L=L,alpha=alpha)

## -----------------------------------------------------------------------------
str(out)

